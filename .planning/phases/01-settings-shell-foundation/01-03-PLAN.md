---
phase: 01-settings-shell-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - dv-control-webapp/public/settings.html
  - dv-control-webapp/public/settings.js
  - dv-control-webapp/public/styles.css
  - dv-control-webapp/test/settings-shell.test.js
autonomous: true
requirements:
  - NAV-02
  - UX-01
  - UX-02
user_setup: []
must_haves:
  truths:
    - "The active settings workspace is noticeably denser on common desktop sizes, reducing scroll distance without hiding essential helper text."
    - "Only one primary settings destination is rendered in the workspace at a time, with section-level group behavior tuned for faster scanning than the current all-open page."
    - "The sidebar and top action area stay usable during scrolling, while the layout still collapses cleanly on narrower screens."
  artifacts:
    - path: "dv-control-webapp/public/styles.css"
      provides: "Compact spacing, sticky shell behavior, and responsive two-column settings layout rules"
      contains: "settings-fields"
    - path: "dv-control-webapp/public/settings.js"
      provides: "Section-level rendering behavior that limits visible workspace content and tunes group defaults"
      min_lines: 250
    - path: "dv-control-webapp/public/settings.html"
      provides: "Shell markup that supports sticky action/sidebar regions and compact orientation blocks"
      contains: "settings"
    - path: "dv-control-webapp/test/settings-shell.test.js"
      provides: "Regression coverage for section-focused rendering and group-default behavior"
  key_links:
    - from: "dv-control-webapp/public/styles.css"
      to: "dv-control-webapp/public/settings.html"
      via: "shell and field-grid classes used for sticky and compact layout behavior"
      pattern: "settings-(grid|sections|fields|group)"
    - from: "dv-control-webapp/public/settings.js"
      to: "dv-control-webapp/public/styles.css"
      via: "renderer applies shell and group classes that the compact layout depends on"
      pattern: "settings-"
---

<objective>
Tighten the Settings shell so it feels more compact and section-focused on desktop while remaining readable and stable on smaller screens.

Purpose: Complete Phase 1 by turning the new shell and taxonomy into a calmer, faster-scanning workspace instead of a merely re-labeled version of the old long page.
Output: Density-focused layout refinements, section-level rendering behavior tuned for lower scroll cost, and smoke coverage for compact shell behavior.
</objective>

<execution_context>
@/Users/christin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-settings-shell-foundation/01-CONTEXT.md
@.planning/phases/01-settings-shell-foundation/01-RESEARCH.md
@.planning/phases/01-settings-shell-foundation/01-VALIDATION.md
@dv-control-webapp/public/settings.html
@dv-control-webapp/public/settings.js
@dv-control-webapp/public/styles.css
</context>

<tasks>
<task type="auto">
  <name>Task 1: Compact the shell layout without losing readability</name>
  <files>dv-control-webapp/public/settings.html, dv-control-webapp/public/styles.css</files>
  <action>Tighten the spacing, orientation blocks, card density, and desktop field layout in the new shell so the Settings page feels materially shorter and easier to scan on common laptop widths. Keep helper text visible, preserve single-column fallbacks on narrower screens, and add sticky treatment for the sidebar and top action region where it improves workflow without covering content.</action>
  <verify><automated>rg -n "position:\\s*sticky|settings-(fields|workspace|sidebar|overview)" dv-control-webapp/public/settings.html dv-control-webapp/public/styles.css</automated><manual>Inspect the updated shell classes in `dv-control-webapp/public/settings.html` and `dv-control-webapp/public/styles.css` to confirm there are explicit sticky and responsive density rules for Settings.</manual></verify>
  <done>The shell has explicit compact-layout rules for desktop and safe fallback rules for tablet/mobile widths.</done>
</task>

<task type="auto">
  <name>Task 2: Tune section-level rendering for faster scanning</name>
  <files>dv-control-webapp/public/settings.js, dv-control-webapp/public/styles.css</files>
  <action>Refine the workspace renderer so the active destination shows a concise orientation header and only its relevant groups, rather than reproducing the old all-open document behavior. It is acceptable to adjust default group expansion to reduce scroll cost, but do not introduce Phase 3-style essential/optional/expert semantics yet. Keep the implementation grounded in the existing field metadata and avoid hiding helper text behind icons or extra clicks.</action>
  <verify><automated>node --check dv-control-webapp/public/settings.js</automated></verify>
  <done>The workspace remains section-focused, visibly calmer than the old full-page renderer, and does not leak unrelated sections back into view.</done>
</task>

<task type="auto">
  <name>Task 3: Lock in compact-shell behavior with targeted smoke coverage</name>
  <files>dv-control-webapp/test/settings-shell.test.js, dv-control-webapp/public/settings.js</files>
  <action>Extend the shell tests to cover section-focused rendering and any extracted helpers for default group-open behavior, then add a short manual smoke checklist to the plan summary or execution notes covering desktop density, sticky sidebar/action behavior, and narrow-width collapse. Keep automated checks fast and let the manual smoke focus only on behaviors that a unit test cannot judge well.</action>
  <verify><automated>node --test dv-control-webapp/test/settings-shell.test.js</automated><manual>Add a short manual smoke checklist covering desktop density, sticky behavior, and narrow-width collapse to the execution notes or summary.</manual></verify>
  <done>Automated smoke coverage guards the rendering rules, and execution leaves behind a concrete checklist for the visual density and sticky-behavior pass.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check dv-control-webapp/public/settings.js`
- [ ] `node --test dv-control-webapp/test/settings-shell.test.js`
- [ ] On a common desktop width, the shell shows a visibly denser workspace with sticky navigation/action support and lower scroll cost than the old Settings page
- [ ] On a narrower viewport, the shell collapses cleanly to a readable single-column flow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Settings feels materially more compact and section-focused while staying readable across desktop and smaller widths
  </success_criteria>

<output>
After completion, create `.planning/phases/01-settings-shell-foundation/01-03-SUMMARY.md`
</output>
