---
phase: 01-settings-shell-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - dv-control-webapp/config-model.js
  - dv-control-webapp/public/settings.js
  - dv-control-webapp/test/settings-shell.test.js
autonomous: true
requirements:
  - NAV-01
  - NAV-03
  - UX-02
user_setup: []
must_haves:
  truths:
    - "The main sidebar uses task-oriented labels that help beginners find the right settings area faster than the current technical taxonomy."
    - "The shell reduces the top-level Settings destinations to roughly five or six beginner-friendly areas, with register-heavy content routed into an advanced destination."
    - "Every existing technical section and field remains reachable through the new taxonomy; no current settings area becomes orphaned."
  artifacts:
    - path: "dv-control-webapp/config-model.js"
      provides: "Task-oriented navigation taxonomy and metadata that stays aligned with the schema-driven config definition"
      contains: "SECTIONS"
    - path: "dv-control-webapp/public/settings.js"
      provides: "Consumption of taxonomy metadata for sidebar labels, overview content, and active section headings"
      min_lines: 250
    - path: "dv-control-webapp/test/settings-shell.test.js"
      provides: "Coverage that every legacy section is mapped into a visible user-facing destination"
  key_links:
    - from: "dv-control-webapp/config-model.js"
      to: "dv-control-webapp/public/settings.js"
      via: "definition metadata consumed by the sidebar and workspace renderer"
      pattern: "getConfigDefinition|definition\\."
    - from: "dv-control-webapp/config-model.js"
      to: "definition.fields"
      via: "section IDs remain the field source of truth while labels/grouping become user-facing"
      pattern: "section:"
---

<objective>
Replace the technical top-level section taxonomy with a compact, task-oriented navigation model that still derives from the existing schema-driven config definition.

Purpose: Deliver the clearer information architecture and beginner-facing wording required by Phase 1 without drifting away from the real field inventory in `config-model.js`.
Output: Updated section metadata and shell rendering that present beginner-friendly group names, orientation copy, and complete section-to-group coverage.
</objective>

<execution_context>
@/Users/christin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-settings-shell-foundation/01-CONTEXT.md
@.planning/phases/01-settings-shell-foundation/01-RESEARCH.md
@.planning/phases/01-settings-shell-foundation/01-VALIDATION.md
@dv-control-webapp/config-model.js
@dv-control-webapp/public/settings.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Encode the new beginner-facing taxonomy in the config model</name>
  <files>dv-control-webapp/config-model.js</files>
  <action>Extend the existing section metadata so the schema can describe the compact Phase 1 destinations described in context and research: a 5-6 item beginner-facing navigation, orientation text for each destination, and a clear place for advanced/register-heavy areas. Keep `getConfigDefinition()` as the single source of truth for section membership; do not create a second hard-coded field map in the frontend that could drift from the schema.</action>
  <verify><automated>node --input-type=module -e "import('./dv-control-webapp/config-model.js').then(({ getConfigDefinition }) => { const definition = getConfigDefinition(); if (!definition.sections || !definition.sections.length) process.exit(1); const ids = definition.sections.map((section) => section.id); if (!ids.includes('system') || !ids.includes('victron')) process.exit(1); })"</automated><manual>Inspect `dv-control-webapp/config-model.js` and confirm every existing technical section is still represented while the exported metadata now supports task-oriented navigation labels and descriptions.</manual></verify>
  <done>The config model can describe both the legacy section inventory and the new user-facing taxonomy without duplicating field ownership outside the schema.</done>
</task>

<task type="auto">
  <name>Task 2: Feed the taxonomy metadata into the shell renderer</name>
  <files>dv-control-webapp/public/settings.js, dv-control-webapp/config-model.js</files>
  <action>Update the shell logic from Plan 01 so the sidebar, overview cards, and workspace headers use the new user-facing taxonomy and section copy instead of raw technical labels. Ensure the rendered destinations stay stable, highlight correctly, and still resolve to the correct field sets in the active workspace.</action>
  <verify><automated>node --check dv-control-webapp/public/settings.js</automated></verify>
  <done>The rendered navigation uses beginner-friendly wording and the active workspace still surfaces the correct config fields for each destination.</done>
</task>

<task type="auto">
  <name>Task 3: Prove full taxonomy coverage with focused tests</name>
  <files>dv-control-webapp/test/settings-shell.test.js, dv-control-webapp/config-model.js, dv-control-webapp/public/settings.js</files>
  <action>Expand the shell test file to assert that every current technical section is assigned to a visible top-level destination, that the navigation count stays within the intended compact range, and that overview/sidebar labels come from the new task-oriented metadata rather than the raw legacy labels.</action>
  <verify><automated>node --test dv-control-webapp/test/settings-shell.test.js</automated></verify>
  <done>Tests fail if a technical section becomes unreachable, if the nav grows back toward the old 10-item list, or if friendly labels disappear.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check dv-control-webapp/public/settings.js`
- [ ] `node --test dv-control-webapp/test/settings-shell.test.js`
- [ ] The sidebar presents about five to six task-oriented destinations and every legacy settings area remains reachable through one of them
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Settings navigation and section labels are task-oriented, compact, and still fully backed by the schema-driven field inventory
  </success_criteria>

<output>
After completion, create `.planning/phases/01-settings-shell-foundation/01-02-SUMMARY.md`
</output>
