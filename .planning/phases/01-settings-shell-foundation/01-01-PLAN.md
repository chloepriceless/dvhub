---
phase: 01-settings-shell-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - dv-control-webapp/public/settings.html
  - dv-control-webapp/public/settings.js
  - dv-control-webapp/public/styles.css
  - dv-control-webapp/test/settings-shell.test.js
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
user_setup: []
must_haves:
  truths:
    - "Settings opens into a shell with a persistent sidebar and a dedicated right-hand workspace instead of one long rendered document."
    - "The default entry state shows an overview in the workspace before the user edits a specific settings section."
    - "Sidebar selection updates the active section reliably and keeps the active destination visibly highlighted."
  artifacts:
    - path: "dv-control-webapp/public/settings.html"
      provides: "Sidebar shell and workspace mount points while preserving the top status/save/service controls"
      contains: "settingsSections"
    - path: "dv-control-webapp/public/settings.js"
      provides: "Active-section state, overview rendering, and shell-aware settings rendering"
      min_lines: 250
    - path: "dv-control-webapp/public/styles.css"
      provides: "Sidebar shell layout, active navigation styling, and workspace sizing rules"
      contains: "settings-"
    - path: "dv-control-webapp/test/settings-shell.test.js"
      provides: "Focused coverage for shell state helpers and overview-first behavior"
  key_links:
    - from: "dv-control-webapp/public/settings.js"
      to: "dv-control-webapp/public/settings.html"
      via: "DOM queries and rendering into shell mount points"
      pattern: "getElementById\\('settingsSections'\\)|getElementById\\(\"settingsSections\"\\)"
    - from: "dv-control-webapp/public/settings.js"
      to: "definition.sections"
      via: "sidebar model and active-section rendering"
      pattern: "definition\\.sections"
---

<objective>
Refactor the existing one-page settings renderer into a sidebar shell with explicit active-section state and an overview-first landing.

Purpose: Establish the structural navigation foundation for Phase 1 without changing backend config semantics or pulling later disclosure/save-work into scope.
Output: A shell-aware Settings page, extracted shell/state helpers in `settings.js`, and targeted shell tests.
</objective>

<execution_context>
@/Users/christin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-settings-shell-foundation/01-CONTEXT.md
@.planning/phases/01-settings-shell-foundation/01-RESEARCH.md
@.planning/phases/01-settings-shell-foundation/01-VALIDATION.md
@dv-control-webapp/public/settings.html
@dv-control-webapp/public/settings.js
@dv-control-webapp/public/styles.css
@dv-control-webapp/config-model.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Bootstrap Wave 0 shell tests and extract testable helpers</name>
  <files>dv-control-webapp/test/settings-shell.test.js, dv-control-webapp/public/settings.js</files>
  <action>Before the UI shell is rebuilt, create the focused `node:test` harness required by the validation strategy and extract any minimal pure helpers needed to test overview-first default state, active-section transitions, and sidebar model construction. Keep the helpers small and local to the shell problem so later tasks can rely on this feedback loop after every commit.</action>
  <verify><automated>node --test dv-control-webapp/test/settings-shell.test.js</automated></verify>
  <done>A Wave 0 shell test file exists and can already validate the initial helper/state contract before UI-heavy tasks begin.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the Settings page into a shell scaffold</name>
  <files>dv-control-webapp/public/settings.html, dv-control-webapp/public/styles.css</files>
  <action>Replace the current single full-width `#settingsSections` flow with a shell scaffold that separates a left navigation column and a right workspace column, but keep the existing top status/save/import/export and Health/Service panels in a compact header region. Add stable class and ID hooks for sidebar items, overview content, active states, and sticky shell behavior so later plans can refine copy and density without reworking the DOM again.</action>
  <verify><automated>rg -n "settings-(shell|sidebar|workspace|overview)" dv-control-webapp/public/settings.html dv-control-webapp/public/styles.css</automated><manual>Open `dv-control-webapp/public/settings.html` and confirm the shell exposes dedicated sidebar/workspace hooks while retaining the existing admin action controls.</manual></verify>
  <done>The HTML and CSS provide a persistent shell structure for Settings and no longer assume the entire form is rendered as one continuous section list.</done>
</task>

<task type="auto">
  <name>Task 3: Split rendering logic around overview and active-section state</name>
  <files>dv-control-webapp/public/settings.js</files>
  <action>Refactor `renderDefinition()` into shell-oriented helpers that can build sidebar destinations, render a default overview state, switch the active destination in place, and render only the selected section into the workspace. Preserve the schema-driven source of truth from `definition.sections` and `definition.fields`; do not hard-code field markup or create a parallel config store. If last-section memory is added, keep the default fresh-entry experience on the overview as required by the phase context.</action>
  <verify><automated>node --check dv-control-webapp/public/settings.js && node --test dv-control-webapp/test/settings-shell.test.js</automated></verify>
  <done>`settings.js` has explicit shell/state boundaries and supports overview-first navigation with visible active-section switching.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check dv-control-webapp/public/settings.js`
- [ ] `node --test dv-control-webapp/test/settings-shell.test.js`
- [ ] Loading Settings shows an overview first, then swaps to a single selected section when a sidebar item is activated
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Settings has a stable shell foundation with overview-first entry and reliable active-section state
  </success_criteria>

<output>
After completion, create `.planning/phases/01-settings-shell-foundation/01-01-SUMMARY.md`
</output>
